# Install required packages
pip install -r requirements.txt

# The application uses SQLite, so no database server is needed.
# The database file (group_tool.db) will be created automatically.

# Run the application
python app.py