# 环境变量配置文件
# 复制此文件为 .env 并填入实际API密钥

# OpenAI API密钥
OPENAI_API_KEY=your_openai_api_key_here

# Google Gemini API密钥
GEMINI_API_KEY=your_gemini_api_key_here

# Anthropic Claude API密钥
ANTHROPIC_API_KEY=your_anthropic_api_key_here

# iFlow API配置
IFLOW_API_KEY=your_iflow_api_key_here
IFLOW_BASE_URL=https://apis.iflow.cn/v1
IFLOW_MODEL_NAME=Qwen3-Coder

# CLI工具配置
# Claude CLI (Anthropic)
CLAUDE_API_KEY=your_claude_api_key_here
CLAUDE_MODEL=claude-3-5-sonnet-20241022

# Codex CLI (OpenAI)
OPENAI_API_KEY=your_openai_api_key_here
OPENAI_BASE_URL=https://api.openai.com/v1
OPENAI_MODEL=gpt-4

# Gemini CLI (Google)
GEMINI_API_KEY=your_gemini_api_key_here
GEMINI_MODEL=gemini-1.5-pro

# OpenCode CLI
OPENCODE_API_KEY=your_opencode_api_key_here
OPENCODE_MODEL=deepseek-coder

# HTTP代理配置（用于访问被限制的API）
# 格式：http://127.0.0.1:7890 或 http://user:pass@proxy.example.com:8080
HTTP_PROXY=http://127.0.0.1:7890
HTTPS_PROXY=http://127.0.0.1:7890
NO_PROXY=localhost,127.0.0.1

# 数据库配置
DATABASE_PATH=experiments.db

# 配置文件路径
AI_RESEARCHER_CONFIG=config/config.yaml

# 应用端口
APP_PORT=20339

# ========================================================================
# RAGFlow知识库配置
# ========================================================================
# RAGFlow是一个开源的RAG（检索增强生成）引擎，用于构建知识库和检索系统
# 配置这些环境变量以连接到RAGFlow服务
#
# ⚠️ 重要：这些端口需要与RAGFlow docker-compose.yml中的配置完全一致！
# 参考RAGFlow官方配置：/home/hrply/software/ragflow/docker/.env
#
# RAGFlow服务端点（根据实际部署情况修改）
# 格式：http://<RAGFlow服务器IP>:<SVR_WEB_HTTP_PORT>
# 例如：http://192.168.3.147:20334
RAGFLOW_ENDPOINT=http://192.168.3.147:20334

# RAGFlow Docker端口映射配置（需要与RAGFlow服务保持一致）
# 这些端口映射在RAGFlow docker-compose.yml中定义
#
# 端口说明：
# - SVR_HTTP_PORT: RAGFlow主要API服务端口（用于API调用）
# - SVR_WEB_HTTP_PORT: RAGFlow Web界面HTTP端口（用于浏览器访问）
# - SVR_WEB_HTTPS_PORT: RAGFlow Web界面HTTPS端口（用于安全访问）
# - ADMIN_SVR_HTTP_PORT: RAGFlow管理服务端口（用于管理API）
# - SVR_MCP_PORT: RAGFlow MCP服务端口（用于模型上下文协议）
#
# 默认值（来自RAGFlow官方配置）：
SVR_WEB_HTTP_PORT=20334
SVR_WEB_HTTPS_PORT=443
SVR_HTTP_PORT=20335
ADMIN_SVR_HTTP_PORT=20336
SVR_MCP_PORT=20337

# RAGFlow API密钥（可选，取决于RAGFlow配置）
# 如果RAGFlow服务配置了API密钥认证，请在此处填写
# RAGFLOW_API_KEY=your_ragflow_api_key_here
